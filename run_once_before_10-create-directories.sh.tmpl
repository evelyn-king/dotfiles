#!/usr/bin/env bash

set -euo pipefail

install -d -m 0755 "${HOME}/local-codex"
install -d -m 0755 "${HOME}/local-journal"
install -d -m 0755 "${HOME}/local-praxis"
install -d -m 0755 "${HOME}/local-codex/environments"
install -d -m 0755 "${HOME}/.local/opt"
install -d -m 0770 "${HOME}/.vim/undo-dir"
