#!/usr/bin/env bash

if command -v bun &>/dev/null; then
  packages=(
    @google/gemini-cli@latest
    @just-every/code@latest
    @openai/codex@latest
    opencode-ai@latest
    @googleworkspace/cli
    @doist/todoist-cli
  )
  bun install -g "${packages[@]}"
fi
